[package]
name = "entity-graph"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Data model for a codebase's entity graph: containment hierarchy plus symbolic references. Zero dependencies; producers fill it, consumers read it."

[features]
# Test fixture helpers (make_entity etc.) shared by downstream crates' tests.
test-support = []

[dependencies]
